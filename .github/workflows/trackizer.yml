name: iOS CI/CD for Trackizer App

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build & Test iOS App
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Xcode
        run: sudo xcode-select -switch /Applications/Xcode.app/Contents/Developer

      - name: Build the app
        run: xcodebuild clean build
        -project "Trackizer.xcodeproj"
        -scheme "Trackizer"
        -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest'


        
#         - name: Build App
#        run: |
#          xcodebuild build-for-testing \
#            -scheme "${{ env.DEFAULT_SCHEME }}" \
#            -workspace "CICDProject1.xcworkspace" \
#            -destination "platform=iOS Simulator,name=iPhone 15" \
#            CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO | xcpretty

      - name: Run tests
        run: xcodebuild test -xcodeproj Trackizer.xcxcodeproj -scheme Trackizer -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest'


